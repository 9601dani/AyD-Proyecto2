server:
  port: 8003
spring:
  profiles:
    active: dev
  application:
    name: ms-user
  datasource:
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 60000
      max-lifetime: 1800000
      connection-timeout: 30000
